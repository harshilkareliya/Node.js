<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TO DO</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        .container {
            margin-top: 50px;
            max-width: 1000px;
        }
        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-weight: bold;
            color: #343a40;
        }
        .form-container {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }
        .task-input {
            width: 700px;
            margin-right: 10px;
        }
        .form-control, .btn, .select {
            border-radius: 30px;
        }
        .table {
            margin-top: 20px;
            border-radius: 10px;
            overflow: hidden;
        }
        .table-hover tbody tr:hover {
            background-color: #e9ecef;
        }
        .btn-sm {
            border-radius: 20px;
        }
        .btn-warning {
            background-color: #ffc107;
        }
        .btn-danger {
            background-color: #dc3545;
        }
        .filter-buttons {
            margin-bottom: 20px;
        }

        .filter-buttons .btn {
            border-radius: 20px;
            margin: 0 5px;
        }

        .btn-filter {
            background-color: #007bff;
            color: white;
            border: none;
        }
        .btn-filter.active {
            background-color: #0056b3;
            color: #fff;
        }

        /* No Tasks Message */
        .no-tasks {
            text-align: center;
            font-size: 35px;
            font-weight: bold;
            color: #6c757d;
            margin-top: 50px;
            
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>TO DO App</h1>
        <div class="form-container">
            <form action="/addtask" method="post" class="d-flex align-items-center">
                <input type="text" name="task" class="form-control task-input" placeholder="Add your task" required>
                <select name="priority" style="width: 150px; margin-right: 10px;height: 40px;border-radius: 50px;">
                    <option value="High">High</option>
                    <option value="Medium">Medium</option>
                    <option value="Low">Low</option>
                </select>
                <button type="submit" class="btn btn-primary" style="height: 38px;">Add Task</button>
            </form>
        </div>
        <br>
        <!-- No Task Label -->
        <div class="no-tasks">
            <p><i class="fa-regular fa-face-frown"></i> No tasks to show!</p>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js" integrity="sha384-oBqDVmMz4fnFO9gybBogGz5xZLJ8eBbbF5a8p2RdweOjnNNISF+0wTQqUOBR8VcQ" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js" integrity="sha384-cuMij68LM8d3a7zWmRmWI/a9lWqXBgV8zEB0DJJ+z4IidDA6mkXQeSjaAqPcK7N0" crossorigin="anonymous"></script>
</body>
</html>
