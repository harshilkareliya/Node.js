<html>
<head>
    <title>Calendar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/style/style.css">
</head>
<body>
    <div class="calendar">
        <div class="calendar-header">
            <h1><%=month%> <%=year%></h1>
            <div class="btn-group" role="group">
                <a type="button" class="btn btn-outline-primary">Month</a>
                <a type="button" class="btn btn-outline-primary">Week</a>
                <a type="button" class="btn btn-outline-primary">Day</a>
            </div>
            <div class="btn-group" role="group">
                <a href="" type="button" class="btn btn-outline-primary"><</a>
                <a href="/" type="button" class="btn btn-outline-primary">Today</a>
                <a type="button" class="btn btn-outline-primary">></a>
            </div>
            
            <button type="button" class="btn btn-primary btn-add">Add Appointment</button>
        </div>
        <table class="calendar-table">
            <thead>
                <tr>
                    <th>Sun</th>
                    <th>Mon</th>
                    <th>Tue</th>
                    <th>Wed</th>
                    <th>Thu</th>
                    <th>Fri</th>
                    <th>Sat</th>
                </tr>
            </thead>
            <tbody>
                <% thisMonth.map((week, weekIndex) => { %>
                    <tr>
                      <% week.map((day, dayIndex) => { %>
                        <td 
                            onclick="<%= (weekIndex === thisMonth.length - 1 && day >= 1 && day <= 6) || (weekIndex === 0 && day >= 23 && day <= 31) ? '' : `showForm(${day})` %>"
                            style="opacity: 
                            <%= (weekIndex === thisMonth.length - 1 && day >= 1 && day <= 6) || (weekIndex === 0 && day >= 23 && day <= 31) ? '0.5' : '1' %>">
                            <%= day %>
                        </td>
                       <% }) %>
                    </tr>
                  <% }) %>                                                   
            </tbody>
        </table>
    </div>
    <!-- <div class="appointment-form" id="appointmentForm">
        <h5 class="mb-3">Add Appointment</h5>
        <form>
            <div class="mb-3">
                <label for="title" class="form-label">Title*</label>
                <input type="text" class="form-control" id="title">
            </div>
            <div class="mb-3">
<<<<<<< Tabnine <<<<<<<
/**undefined+
 * Function to add a new appointment to the database.undefined+
 *undefined+
 * @asyncundefined+
 * @function addAppoitmentundefined+
 * @param {Object} req - The request object containing the appointment data.undefined+
 * @param {Object} res - The response object to send back to the client.undefined+
 * @param {Object} req.body - The appointment data to be added.undefined+
 * @param {string} req.body.title - The title of the appointment.undefined+
 * @param {string} req.body.description - The description of the appointment.undefined+
 * @param {Date} req.body.date - The date of the appointment.undefined+
 * @param {string} req.body.time - The time of the appointment.undefined+
 * @returns {Promise} - A promise that resolves to the result of adding the appointment.undefined+
 * If the appointment is added successfully, the promise resolves to the added appointment data.undefined+
 * If there is an error adding the appointment, the promise rejects with an error message.undefined+
 */undefined+
module.exports.addAppoitment = async (req, res)=>{undefined+
>>>>>>> Tabnine >>>>>>>undefined {"conversationId":"d749c64e-9cbb-44cc-a7d9-230f409ad696","source":"instruct"}
                <label for="date" class="form-label">Date*</label>
                <div class="input-group">
                    <input type="text" class="form-control" id="selectedDate" readonly>
                </div>
            </div>
            <div class="mb-3">
                <label for="start-time" class="form-label">Start Time*</label>
                <input type="text" class="form-control" id="start-time">
            </div>
            <div class="mb-3">
                <label for="end-time" class="form-label">End Time*</label>
                <input type="text" class="form-control" id="end-time">
            </div>
            <div class="d-flex justify-content-between">
                <a href="/" class="btn btn-link">Cancel</a>
                <a href="#" class="btn btn-link">Add Appointment</a>
            </div>
        </form>
    </div> -->
    <div class="overlay" id="overlay"></div>

    <!-- Appointment Form -->
    <div class="appointment-form" id="appointmentForm">
        <h5 class="mb-3">Add Appointment</h5>
        <form method="post" action="/addAppoitment">
            <div class="mb-3">
                <label for="title" class="form-label">Title*</label>
                <input type="text" name="title" class="form-control" id="title">
            </div>
            <div class="mb-3">
                <label for="date" class="form-label">Date*</label>
                <div class="input-group">
                    <input type="text" name="date" class="form-control" id="selectedDate" readonly>
                </div>
            </div>
            <div class="mb-3">
                <label for="start-time" class="form-label">Start Time*</label>
                <input type="time" name="startTime" class="form-control" id="start-time">
            </div>
            <div class="mb-3">
                <label for="end-time" class="form-label">End Time*</label>
                <input type="time" name="endTime" class="form-control" id="end-time">
            </div>
            <div class="d-flex justify-content-between">
                <a href="/" class="btn btn-link">Cancel</a>
                <input type="submit" class="btn btn-link" value="Add Appointment"></input>
            </div>
        </form>
    </div>
</body>
</html>
<script>
    // JavaScript to show the form and set the selected date
    function showForm(day) {
    // Show the overlay and form
    document.getElementById('overlay').style.display = 'block';
    document.getElementById('appointmentForm').style.display = 'block';

    // Set the selected date
    let selectedDate = day + '/<%= monthNumber %>/<%= year %>';
    document.getElementById('selectedDate').value = selectedDate;
}

</script>